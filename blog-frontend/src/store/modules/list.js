/**
 * 포스트 목록 상태를 다룸
 */

import {ceateAtion, handleActions} from 'redux-actions'
import {Map} from 'immutable'
import {pender} from 'redux-pender'

const initialState=Map({})

export default handleActions({

},initialState)